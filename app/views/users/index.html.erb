<table>
  <tr>
    <th>name</th>
    <th>edit</th>
  </tr>
  <% @users.each do |user|%>
    <tr>
      <td><%= user.username %></td>
      <td><%= link_to 'edit', edit_user_path(user) %></td>
    </tr>
  <% end %>
</table>
